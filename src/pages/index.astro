---
// Фронтматтер
import CalorieCalculator from '../components/CalorieCalculator.astro'; 
import Lightbox from '../components/Lightbox.astro'; 

// 1. ПРЯМОЙ СТАТИЧЕСКИЙ ИМПОРТ ИЗОБРАЖЕНИЙ
// Это самый чистый способ получить правильный путь от Vite.
import chickenImg from '../assets/images/chicken-free.webp';
import veganImg from '../assets/images/vegan-free.webp';
import salmonImg from '../assets/images/salmon-free.webp';
import sportImg from '../assets/images/sport-free.webp';
import heroImg from '../assets/images/hero-free.webp';

// 2. СТРУКТУРА ДАННЫХ
const mealsData = [
 { name:"Курица + овощи", kcal:420,p:42,f:8,c:20, img: chickenImg },
 { name:"Веган боул", kcal:360,p:18,f:12,c:45, img: veganImg },
 { name:"Лосось + овощи", kcal:390,p:34,f:26,c:8, img: salmonImg },
 { name:"Фитнес-питание", kcal:450,p:35,f:15,c:30, img: sportImg }
];

// 3. ПЕРЕМЕННАЯ ДЛЯ ФОНА
const heroImage = heroImg;
---

<!DOCTYPE html>
<html lang="ru">
<head>
    <style>
    /* ... (ВСЕ СТИЛИ ВАШЕГО CSS ЗДЕСЬ) ... */
    </style>
</head>

<body>

<header id="hero" style={`background-image:url(${heroImage})`}> 
</header>

<section>
<h2 style="text-align:center;margin-bottom:20px">🍽️ Блюда</h2>
<div class="meals" id="meals">
    {mealsData.map(meal => (
    <div class="meal">
      <img src={meal.img} onclick={`openLightbox('${meal.img}')`} alt={meal.name}>
      <div>
        <h3>{meal.name}</h3>
        <p>{meal.kcal} ккал • Б:{meal.p} Ж:{meal.f} У:{meal.c}</p>
      </div>
    </div>
  ))}
</div>
</section>

</body>
</html>
